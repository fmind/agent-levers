# Lever — `<id>-<slug>`

## TL;DR

- **Goal:** <one sentence — the intent in plain English.>
- **Success sketch:** <one line — what a human would observe when this works.>
- **Out of scope:** <one line — explicit non-goals.>
- **Open for plan:** <count> question(s) for plan to investigate; <count> escalated to user (or "none").
- **State:** `step: plan` — brief captured; ready to plan.
- **Next:** `/lever <id>` to plan.

## Brief

### Goal

<one paragraph — full intent, paraphrased only when ambiguous. Close with explicit non-goals: "X, Y, and Z are out of scope for this task.">

### Context

- <fact or constraint> — source: `<file>` / project convention / user message.
- <defaulted choice> — defaulted from `<source>`.

### Success sketch

- <what a human would observe when this works — informal, plain English, not mechanism>
- <another observable signal, when there's more than one>

### Open questions

- <question that needs codebase investigation, not a user answer>
